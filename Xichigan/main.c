#include "Xichigan.h"

/*********************************************************************************
* Author: Laine Rumreich								       				 				   *
*																				 							*
*																				 							*
* How to compile: make										 					 					*
* How to run: ./xichigan                 									 	 				*
**********************************************************************************/

int main(int argc, char*argv[]) {
	//int *currentThread;
	//int numThreads, i, j, k, x, n, m, p, index;

	/* Initialize Shared Variables and Threads */
	//threads = (pthread_t *)malloc(sizeof(pthread_t)*numThreads);
	//currentThread = (int *)malloc(sizeof(int)*numThreads*numThreads);

	XtoM();

	return 0;
}


